# Dependencies
node_modules/

# Build output.
# NOTE: docs/ is intentionally NOT ignored — it is committed so GitHub Pages
# can serve it via "Deploy from a branch -> /docs".
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
