# This project uses only public open-source packages, so it pulls from the
# public npm registry. This overrides any global/corporate registry setting
# for THIS project only (your global ~/.npmrc is untouched).
registry=https://registry.npmjs.org/
